/**
 * Vitest global test setup for @stackra/ts-container.
 *
 * Runs before every test file. Use for:
 * - Global mocks
 * - Polyfills
 * - Test environment configuration
 * - Metadata cleanup
 *
 * @module @stackra/ts-container/test-setup
 */

import "reflect-metadata";
