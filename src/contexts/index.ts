/**
 * React Contexts Barrel Export
 *
 * - {@link ContainerContext} — React context holding the DI container resolver
 *
 * @module react/contexts
 */

export { ContainerContext } from "./container.context";
