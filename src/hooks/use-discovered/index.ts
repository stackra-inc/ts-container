/**
 * useDiscovered Hook Barrel Export
 *
 * - {@link useDiscovered} — Get providers tagged by a discoverable decorator
 *
 * Companion type lives at `@/interfaces/use-discovered-options.interface.ts`
 * (one export per file).
 *
 * @module hooks/use-discovered
 */

export { useDiscovered } from "./use-discovered.hook";
