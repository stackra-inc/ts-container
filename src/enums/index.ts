/**
 * Enums Barrel Export
 *
 * All enums, type aliases, and enums for the DI container.
 * These define the public API surface and contracts that consumers
 * and library authors depend on.
 *
 * - {@link Scope} — Enum defining provider lifecycle scopes (DEFAULT, TRANSIENT)
 *
 * @module enums
 */
export { Scope } from './scope.enum';
